<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.webexam.mapper.WebLessonMapper">

    <select id="queryAll" resultType="WebLesson">
        SELECT `id` AS id,
                `lesson_name` AS lessonName,
                `lesson_url` AS lessonUrl
        FROM `t_web_lesson`
    </select>
</mapper>